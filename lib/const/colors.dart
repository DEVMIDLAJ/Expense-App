import 'package:flutter/material.dart';

// Primary color for the application
const kfirstColor = Color(0xFF713FC6);

// Secondary color with RGBA values
const ksecondColor = Color.fromRGBO(238, 229, 255, 1);

// Black color constant
const kblack = Colors.black;

// White color constant
const kWhite = Colors.white;

// Grey color constant
const kgrey = Colors.grey;

// Color for representing income
const incomeColor = Colors.green;

// Color for representing transaction (assuming a neutral blue)
const transactionColor = Colors.blue;

// Color for representing expenses
const expenseColor = Colors.red;
